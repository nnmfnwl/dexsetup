# dexsetup try to reserve 2 CPU cores for other system resources
cc_cpu_cores_reserve=2
# even if CPU has only 2 CPU cores which supposed to be all reserved by above variable, it use them both because minimal is 2
cc_cpu_jobs_min=2
